[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "workman"
version = "0.1.0"
dependencies = ["typer>=0.12", "httpx>=0.27", "rich>=13"]

[project.scripts]
workman = "workman.main:app"
