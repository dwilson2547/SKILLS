---
name: context-store
description: >
  DEPRECATED — the AI Playbooks/context-store service is retired. Structured documents
  (plans, strategies, procedures) are plain markdown files in the repo: domain-wide guidance
  in <domain>/docs/topics/, project docs in <project>/docs/. Do not ingest to or query
  http://localhost:8001. If invoked, follow the workspace-conventions skill instead.
---

# Playbooks — RETIRED (repo-native now)

> ⚠️ Retired 2026-07. All playbook content was exported to the workspace repo. Write
> structured documents directly to `<domain>/docs/topics/` or `<project>/docs/` and keep
> them current there. See the workspace-conventions skill and CONVENTIONS.md §5.
