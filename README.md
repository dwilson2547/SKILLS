# SKILLS

Canonical skill repository path in this workspace: `meta/SKILLS`.

This repo is used to populate runtime skill directories via symlinks:

- `~/.agents/skills`
- `~/.claude/skills`

Install or refresh links with:

`./meta/SKILLS/install_skill_symlinks.sh`
